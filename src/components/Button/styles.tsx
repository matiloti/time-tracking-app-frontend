import { StyleSheet } from "react-native";

const defaultStyle = StyleSheet.create({
    button: {
        backgroundColor: 'limegreen',
        borderRadius: 10,
        padding: 10,
    },
    text: {
        color: 'white',
        fontWeight: 'bold',
        textAlign: 'center',
        fontSize: 16
    }
});

export { defaultStyle };

